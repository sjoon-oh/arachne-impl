"""arachne

Python-side tooling for the arachne project: workload generation
(arachne.workload), benchmark orchestration (arachne.benchmark), and log
analysis (arachne.analysis). The C++ runtime itself lives under cpp/.
"""
